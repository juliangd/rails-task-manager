<h2><%= @task.name %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed ? "✅ - task is completed" : "🈹 - task is not completed" %> </p>

<%= link_to "Delete", task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>


      <%= link_to "Edit task", edit_path  %>

<p><%= link_to "Back to list", tasks_path  %></p>
<p><%= link_to "Create a new task", new_path  %></p>

